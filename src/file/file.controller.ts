/*import { Controller, Post, Body, Post, UseInterceptors, UploadedFile, } from '@nestjs/common';
import { FileFieldsInterceptor } from '@nestjs/platform-express';

@Post('upload')
@UseInterceptors(FileFieldsInterceptor([
  { name: 'avatar', maxCount: 1},
  { name: 'sbu', maxCount: 1},
]))
UploadedFile(@UploadedFile() files: { avatar?})*/
